# Week12pt3
1. A linked list is most useful when you need to effectiently insert and delete elements
2. A stack follows last-in first-out while a queue is a first in first out
3. A developer would use a stack to put in an undo button
4. They would use an array when they need to quickly access elements Arrays are more space efficient.
